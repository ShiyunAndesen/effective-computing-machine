//static int g_val = 2020;//全局变量
//static修饰全局变量，改变了变量的作用域——让静态的全局变量只能在自己所在的源文件内部使用（变量生命周期变短/小）

/*
static int Add(int x, int y)//static修饰函数，改变了函数的连接属性——外部属性—>内部属性
{
	int z = x + y;
	return z;
}
*/
